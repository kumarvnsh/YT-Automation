# Copy this file to channels/astrotold/.env and supply channel-local credentials.
# load_config(channels/astrotold/config.yaml) reads this .env, not the root .env.

# Required shared providers
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
PEXELS_API_KEY=

# Optional providers (only if their provider is enabled in config.yaml)
# ELEVENLABS_API_KEY=
